# Connect to Supabase via connection pooling with Supavisor.
# This is recommended for production use.
# Get this URL from your Supabase project settings under "Database" → "Connection Pooling".
DATABASE_URL=

# Direct connection to the database. Used for migrations.
# You can find this in Supabase under "Database" → "Connection Parameters".
# Only use this for running migrations, NOT in production code.
DIRECT_URL=

# Public Supabase API URL
# Get this from your Supabase project settings under "API" → "Project URL".
NEXT_PUBLIC_SUPABASE_URL=

# Public Supabase Anonymous Key
# This key is found in Supabase under "API" → "Project API Keys" → "anon public".
# It's safe to use on the client-side but has limited permissions.
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Resend API Key (for sending emails)
# Create an account at https://resend.com/ and generate an API key.
# Add the key here to enable email sending features.
NEXT_PUBLIC_RESEND_API_KEY=
